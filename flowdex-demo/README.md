# Flowdex incident digest demo

This crate starts with two deliberately incomplete functions. The Flowdex demo workflow implements them in parallel, verifies the result, reviews the integrated change, and stops at a human boundary.
